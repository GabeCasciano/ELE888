function [w] = BackPropigation(numberOfHidden, numberOfInputs, numberOfOutputs, eta, theta, x1, x2, t)
w = zeros(numberOfInputs);
hidden = zeros(numberOfHidden);
output = zeros(numberOfOutputs);
bias = zeros(numberOfHidden + numberOfOutputs);


end
